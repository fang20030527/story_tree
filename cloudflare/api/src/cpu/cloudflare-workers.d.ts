// Replaced by Wrangler-generated runtime types when the CPU binding is enabled.
// Only the superclass constructor is needed by this isolated module.
declare module 'cloudflare:workers' {
  export class DurableObject<Env = unknown> {
    constructor(ctx: unknown, env: Env);
    protected readonly env: Env;
  }
}
