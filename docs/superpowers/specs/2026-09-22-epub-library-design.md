# EPUB 外刊批量导入

用户已确认：将 awesome-english-ebooks 的全部 EPUB 拆为单篇外刊，沿用应用内概述、阅读、查词、书架及进度功能。

以仓库提交 `31d15f6ae17b7ca1c622ed8488e11e9cf6669f8c` 为固定来源，共 215 本 EPUB。使用 NCX 目录叶节点确定文章边界和顺序，解析 EPUB 内 XHTML 和图片，不执行脚本。去除封面、栏目索引、广告及下载器导航，保留原文段落、标题、图表与图片。原始下载仅缓存到被 Git 忽略的目录。

生成轻量文章索引、按期刊存储的正文和压缩图片，文章正文按需载入。图片存放在 `server/assets/editorial/epub`，通过只接受 24 位摘要文件名的公共图片接口按需加载和缓存，避免将约 1.3 GB、17,000 多张图片交给 Metro 打包导致 EMFILE。完整性报告记录每本的源路径、Git blob、目录篇数、导入篇数及错误。原包仅含导航、没有正文的条目单独记录，不生成空白文章；遇到无法解析的期刊必须报告失败，不静默跳过。

已有 2026-09-19 经济学人保持原 ID 和人工中文校订，不重复导入。其他文章保留原文标题、摘要，阅读难度明确标为英文原版，不伪造翻译或难度评级。列表沿用原文章卡片，分批显示并支持搜索、刊物和年份筛选。

验收：215 本全部有记录；每本目录与导入篇数加缺正文条目数一致；ID 唯一且不超过 64 字符；全部生成图片可解析，源包损坏引用明确报告；旧文章回归；列表筛选和分页；图片接口路径约束、缓存及不存在资源响应；全仓 npm run check；Expo Web 导出。
